{
  "name": "@flyo/nitro-astro-workspace",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "./lib",
    "./playground"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=lib & npm run playground",
    "libbuild" : "npm run build --workspace=lib",
    "playground": "npm run dev --workspace=playground"
  }
}
